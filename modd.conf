**/*.go {
  # prep: go vet @dirmods
  prep: go test @dirmods
  # prep: go build -o learngo
  # daemon +sigterm: ./learngo
}

# Exclude all test files of the form *_test.go
**/*.go !**/*_test.go {
  prep: go build -o learngo .
  daemon +sigterm: ./learngo
}